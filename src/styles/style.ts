import { makeStyles, createStyles } from '@material-ui/styles';

export const styles = makeStyles(() =>
	createStyles({
		root: {
			background: 'red',
			color: 'white',
			padding: '20px',
		},
	})
);
